
import { shortenText } from 'mhadaily-frontend-test/helpers/shorten-text';
import { module, test } from 'qunit';

module('Unit | Helper | shorten text');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = shortenText([42]);
  assert.ok(result);
});

